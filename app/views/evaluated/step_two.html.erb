<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <div class="container margin-top">

      <h3><%= link_to "Ser evaluado", evaluated_path, class:"myh-link" %><span style="color: rgb( 92, 148, 74);"> >> Paso 2</span></h3>
      <h5 class="myh">Fija tus objetivos</h5>

      <br>
      <h5>Chachara</h5>
      <br>

      <% counter = 0 %>
      <% if @goals == [] %>
        <p>No tienes objetivos</p>
      <% else %>
        <% @goals.each do |g| %>
          <p><%= g.perspective %></p><br>
          <p><%= g.weight %></p><br>
          <% counter += g.weight %>
        <% end %>
        <br>
      <% end %>

      <p><%= @user.goals.get_total_weight %></p>

      <div style="row">
        <div class="col-md-offset-8 col-md-4">
          <h4>
            <%= link_to "Crear", new_goal_path, class:"btn" %>
            <%= link_to "Aceptar", evaluated_path, class:"btn" %>
          </h4>
        </div>
      </div>

    </div>
  </body>
</html>
